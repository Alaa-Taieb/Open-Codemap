# Voyage AI — default embedder (voyage-code-3). Leave blank to use the OSS fallback or mock.
VOYAGE_AI_API_KEY=

# Jina — OSS fallback embedder (jina-embeddings-v3). Leave blank for mock in tests/CI.
JINA_API_KEY=
